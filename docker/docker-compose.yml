version: "3"
  
services:
  pingo:
    container_name: pingo
    image: zhanws/pingo:latest
    restart: always
    privileged: true
    environment:
      - DISPLAY=$DISPLAY
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /dev/snd:/dev/snd
    ports:
      - 5001:5001

